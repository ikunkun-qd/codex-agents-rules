# AGENTS.md

## 0. 适用定位与全局规则

这份规则面向个人 AI 协作场景：`.agent-docs/` 个人文档是给 AI 自己看的备忘、契约和状态记录，用于跨会话续作和排障，不是团队共享文档。

- 临时生成的测试文件和脚本，任务完成后要删除；启动的 subAgent 在任务完成时也要及时关闭。
- 个人契约与文档核心记录本机 Agent 自己实现的接口和决策；对接他人或项目既有接口时，直接阅读对方源码和文档确认实际行为，不猜不虚构；实测验证过的调用片段与参数样例可作为集成备忘记入笔记或契约附录，避免下次会话重复通读源码。

## 1. 新会话与断点续作

开始处理非只读任务或继续已有任务前，必须先检查：

1. `README.md`
2. `.agent-docs/README.md`（个人文档索引，存在时先读）
3. 项目状态文档（已有则只读参考；没有则按“长期项目状态”规则判断是否在个人 `.agent-docs/` 目录创建）
4. 状态文件指向的活动 PRD、功能规格、设计文档、架构文档和实施计划
5. 与当前任务相关的 API/数据契约、ADR、数据字典、Schema、Migration、代码和测试
6. Git状态、当前Diff和最近相关提交

若用户提出新的独立任务，以新任务为准。当用户明确要求继续当前项目但未指定计划时：若Superpowers可用且只有一个未完成实施计划，则使用其 `subagent-driven-development` 工作流按该计划续作；存在多个候选、记录冲突或无法判断时才询问用户。Superpowers未安装时先按第14节帮助用户从GitHub安装并配置；已安装但不可用、没有未完成计划或计划状态无法判断时，改用`PROJECT_STATUS.md`的“唯一下一步”和Git记录续作；使用Superpowers期间以其计划为准，不与个人`.agent-docs/plans/`并行维护两套计划，任务结束把结果同步回`PROJECT_STATUS.md`。

执行前确认当前分支、活动计划、已完成项、阻塞项、下一步和未提交修改。文档可能过期，必须用代码、测试和Git交叉验证；不重复已有证据证明完成的任务。

事实来源：

- 目标行为：已批准的 PRD、规格、API契约、ADR和数据字典。
- 当前实现：代码、配置、Schema、Migration、测试和实际运行结果。
- 执行进度：`PROJECT_STATUS.md`和Git记录；Superpowers规格和计划只表示目标与步骤，不单独证明已经完成。
- 来源冲突时列出差异及影响；无法确认的信息标记为 `UNKNOWN`，不自行猜测。

项目已有文档、目录或命名规范时只读参考。需要纳入个人文档体系时，复制到当前项目个人 `.agent-docs/` 目录下的对应位置，后续只维护副本，不修改原有文件。

## 2. 长期项目状态

预计持续多个会话或包含多个计划的项目，在当前项目个人 `.agent-docs/` 目录维护状态文档；已有项目状态文件只读参考，需要个人记录时复制到 `.agent-docs/project/PROJECT_STATUS.md`。一次性任务和纯只读问答无需创建或更新。

个人状态文件不存在时，在首次实施前根据已验证的仓库状态创建最小版本，只记录：

- 当前版本或分支、需求来源、活动 PRD/设计/实施文档链接、当前任务
- 最近完成、进行中、阻塞项、唯一下一步（当前最优先的单个动作，存在阻塞时即解除阻塞的动作）、最近验证
- 关联 commit、PR 或其他变更记录、负责人和更新时间

仅在活动计划、当前任务、阻塞项、下一步或验证结果发生实际变化时更新。每次任务结束时，必须在完成记录中登记关联 commit、PR 或未提交变更状态。结束或清理Superpowers创建的工作区前，先同步完成项、遗留问题、下一步和最近验证。

状态文件只保存当前摘要和文档指针；完整需求、设计、执行细节和历史分别保留在规格、计划、ADR和Git中。

## 3. 项目知识图

大型、长期或多模块项目，若Understand Anything可用，使用其知识图作为模块索引：

- 已有 `.ua/knowledge-graph.json` 或 `.understand-anything/knowledge-graph.json` 时只读参考；需要保留个人快照时复制到当前项目个人 `.agent-docs/` 目录，不修改知识图源文件。已有人工模块文档同样只读参考。知识图记录项目结构和依赖，`PROJECT_STATUS.md`记录执行进度，二者不互相替代。
- 知识图缺失时：现有项目在首次功能迭代或Bug修复前可运行 `$understand`；新项目在主要目录和模块形成后可运行。若工具会把结果写入项目文件，必须将输出复制或导出到个人 `.agent-docs/` 目录，不得直接更新已有项目文件；无法安全输出时改用代码搜索、调用链、Git和测试。语言默认使用中文，需要额外完整性复核时才加 `--review`；空仓库、一次性小任务和纯只读问答可跳过。首次扫描前检查 `.understandignore` 和项目现有忽略规则。
- 功能迭代或Bug排查前，先用 `$understand-chat <功能或问题>` 定位入口、相关模块、上下游依赖和测试，再读取实际代码、契约、日志和测试；知识图只用于导航和影响分析，不作为当前实现的唯一证据。
- 知识图落后于当前提交时，先以只读方式检查；需要更新个人快照时，将结果复制或导出到个人 `.agent-docs/` 目录，不直接修改已有知识图。工作区存在未提交修改时，将知识图视为基线，并同时检查Git Diff和实际代码。
- 修改完成后使用 `$understand-diff` 检查影响范围。工具未安装时先按第14节帮助用户从GitHub安装并配置；安装失败、执行失败或图不完整时，改用代码搜索、调用链、Git和测试继续定位。
- `.ua/`或`.understand-anything/`是否提交、忽略哪些内容以及是否启用 `$understand --auto-update`，遵循项目现有规范；未经确认不修改Git钩子或提交生成物。

本节提到的命令名、参数和目录以工具实际安装版本为准；不一致时先用工具帮助确认再继续，不因命令差异中断任务。

## 4. 数据库设计

涉及表、字段、索引、约束或关系变化时，同一任务必须同步：

- Schema或ORM模型
- Migration及回滚方案；不可逆时明确说明
- `.agent-docs/data/DATA_DICTIONARY.md`
- 受影响的规格、API契约、共享类型和自动测试

数据字典至少记录表名、字段名、字段类型、可空与默认值、键与索引、业务用途、数据来源、设计原因、约束、敏感性、关联接口/API、对应 migration、迁移兼容性及关联任务。另行说明旧数据回填，以及迁移对性能、锁和并发的影响。遵循现有迁移规范，不直接修改已发布Migration。

## 5. API与架构决策

API变化必须同步现有契约、共享类型、请求响应、错误码、认证权限、兼容性、示例和契约测试。前端不得虚构后端字段；后端不得在未更新契约时改变响应。破坏性变更实施前必须确认。

个人契约以本机 Agent 实现的接口为主，不为他人或既有接口维护全套正式契约。对接他人或既有接口时，直接阅读对方源码或文档确认实际行为，并把实测验证过的调用片段、参数样例和错误码作为集成备忘记入笔记或契约附录，避免下次会话重复通读。

引入重要生产依赖，或改变核心数据模型、模块边界、认证权限、基础设施时，创建 `.agent-docs/architecture/adr/ADR-NNN-<title>.md`，记录背景、决策、原因、备选方案、影响、风险、回滚方式和关联任务。普通实现细节保留在项目规格或实施计划中。

## 6. PRD、架构与实现追踪

重大新功能、重要行为变化和架构调整必须遵循以下完整链路（一般小改动按第7节的文档分级走轻量路径，不强制补齐全套文档）：

```text
PRD → 功能规格 → 架构设计与 API/数据契约 → 实施计划 → 代码/迁移与测试 → 验收记录
```

PRD是目标需求的最高来源。适用时，每个规格、设计文档、API/数据契约、数据字典、实施计划和验收记录必须标注对应的PRD条目；代码模块、API、数据库变化和测试应能反向追溯到对应的规格或PRD。

实施前必须检查现有PRD、规格、架构节点、模块、API/数据契约、数据字典、测试和运行资料，确认需求是否已有实现，避免重复建设。

新增功能必须说明其归属的架构节点、依赖关系、影响范围和非目标。架构发生变化时，必须同步更新架构文档和相关规格。

涉及需求、架构、公共接口、数据结构、模块边界或生产依赖变化时，Agent必须先更新对应的规格、架构文档、API/数据契约、数据字典或实施计划，再修改代码。若技术方案、第三方库特性或数据结构尚不确定，允许在明确告知用户进行“探索性试验（Spike/PoC）”的前提下编写最小原型代码进行可行性验证；验证结论明确后，必须先固化/更新相关规格、架构或契约文档，再推进正式编码与合并。

代码修改完成后，Agent必须根据实际实现结果更新验收记录、验证结果、未覆盖范围和剩余风险，并保留相关文档链接。

## 7. 设计文档与实施文档

设计文档和实施文档承担不同职责，不能用其中一类替代另一类：

- **设计文档**回答“做什么、为什么做、边界在哪里”。至少记录目标、非目标、业务流程、架构节点、模块职责、依赖关系、接口或数据变化、关键决策、异常场景和验收标准。重要架构决策另用 ADR 记录。
- **实施文档**回答“按什么步骤落地”。至少记录前置条件、任务拆分、修改模块或文件、数据/API 变更、测试方案、发布与回滚步骤、风险、负责人和完成状态。涉及数据库变更时，还必须写明 Schema/Migration、数据回填、兼容性、验证和回滚步骤。
- **数据字典**（默认路径为 `.agent-docs/data/DATA_DICTIONARY.md`）属于设计/契约类的专门文档，记录当前认可的数据模型及其业务语义。它不替代功能设计文档、架构设计或实施文档；数据设计文档说明原因和影响，实施文档说明如何迁移，数据字典记录最终字段、约束和关联关系。
- 设计文档和实施文档必须相互引用，并标注对应的 PRD 条目、需求 ID 或架构节点。实施过程发生偏差时，必须回写实施文档；影响设计或架构的偏差还必须更新设计文档或 ADR。

当前项目的个人文档目录固定为 `.agent-docs/`，只由本人和 AI 维护，所有项目通用：它是隐藏目录，与项目已有的 `docs/` 等目录天然不冲突，任何项目都不修改其 `.gitignore` 和已有目录。为避免出现在 `git status` 或被误提交，首次创建前将 `.agent-docs/` 一行加入仓库本地的 `.git/info/exclude`（只在本机 clone 生效，不进版本库、不影响他人）。个人目录不进 Git，是整套体系里唯一没有版本备份的资料：代码丢失可以重新 clone，这些文档丢失后断点续作信息无法恢复，必要时定期自行备份。

项目已有文档、目录、模板和编号规则只读参考；需要纳入个人文档体系时，复制到个人文档目录对应位置，后续只维护副本，不修改源文件。没有可参考的目录时，按以下默认目录创建；只创建当前项目和任务实际需要的文档，不为可选目录创建空文件：

```text
.agent-docs/
├─ README.md                         # 文档索引、阅读顺序和目录约定
├─ prd/                              # 产品需求、范围、用户场景和验收目标
│  └─ PRD-<id>-<slug>.md
├─ specs/                            # 功能规格、详细设计和技术方案
│  └─ SPEC-<id>-<slug>.md
├─ architecture/                     # 架构总览、模块边界和依赖关系
│  ├─ ARCHITECTURE.md
│  └─ adr/                           # 重要架构决策及其原因、影响和回滚
│     └─ ADR-NNN-<title>.md
├─ api/                              # API/事件/共享类型契约和示例
│  └─ API_CONTRACT.md
├─ data/                             # 数据模型、字段语义和迁移关联
│  ├─ DATA_DICTIONARY.md
│  └─ migrations/                    # 仅在项目约定将迁移文档放在 docs 时使用
├─ plans/                            # 实施计划、任务拆分和执行状态
│  └─ PLAN-<id>-<slug>.md
├─ project/                          # 跨会话状态和项目级索引
│  └─ PROJECT_STATUS.md
├─ testing/                          # 测试策略、测试矩阵和验收测试说明
│  └─ TEST_STRATEGY.md
├─ operations/                       # 部署、回滚、运行手册、监控和 SLO
│  ├─ DEPLOYMENT.md
│  ├─ RUNBOOK.md
│  └─ OBSERVABILITY.md
└─ acceptance/                       # 需求验收、验证结果和遗留风险
   └─ ACCEPTANCE-<id>-<slug>.md
```

目录中各类文档的职责如下：`prd/` 保存业务目标，`specs/` 保存功能设计，`architecture/` 保存架构树和 ADR，`api/` 与 `data/` 保存公共契约和数据语义，`plans/` 保存实施步骤，`project/` 保存跨会话状态，`testing/` 保存测试策略，`operations/` 保存上线后的运行资料，`acceptance/` 保存最终验收证据。`.agent-docs/README.md` 只做索引，不替代任何专业文档。

各类文档的 `<id>` 在没有现成编号规则时使用递增序号；跨文档引用时直接写文件名链接，不依赖裸编号。

文档要求按改动影响分级，默认从轻量档开始，影响范围扩大时向上升级，不确定时就高不就低：

- **轻量（默认）**：Bug修复、配置或文案调整、不影响接口和数据模型的小改动——只在 `PROJECT_STATUS.md` 登记任务、根因和验证结果；接口或字段确有变化时顺手补记到个人契约或数据字典，不新建规格、计划和验收文档。
- **标准**：影响单个模块的普通新功能——先写简短的功能说明（目标、边界、验收标准）和实施记录再动代码；涉及自己接口的同步更新契约和数据字典。
- **完整**：涉及架构、公共接口、数据模型、模块边界、认证权限或生产依赖的新功能和调整——走 PRD → 规格 → 架构/契约 → 实施计划 → 实现 → 验收全链路，重要决策另立 ADR。

文档初始化和补缺按项目类型执行：

- **已有项目**：先检查公司规则、已有 PRD、设计文档、实施计划、架构文档、状态文件、契约、测试和运行资料；这些文件只读参考。需要纳入个人文档体系时，复制到个人 `.agent-docs/` 目录下的对应位置，后续只维护副本，不修改源文件。项目预计跨多个会话、计划或模块且没有个人状态文件时，在首次实施前创建 `PROJECT_STATUS.md`（无既有路径约定时默认使用 `.agent-docs/project/PROJECT_STATUS.md`）；一次性小改动和纯只读任务可以不创建。涉及上线、数据、公共 API 或重要架构变化时，按目录补齐个人的契约、数据、运行或 ADR 文档。
- **新建 0-1 项目**：按项目规模分级准备前置文档。小型脚本、探索原型或个人 MVP 项目，仅需创建单文件需求与设计说明，并初始化 `PROJECT_STATUS.md`，不强制拆分全套目录；中大型或正式生产级项目，在首次编写业务代码前创建 `.agent-docs/README.md`、PRD 或经确认的需求记录、功能设计文档、架构总览、首期实施计划和 `PROJECT_STATUS.md`。涉及公共 API、数据模型、认证、基础设施或重要生产依赖时，按需补齐 API/数据契约、数据字典或 ADR；准备部署或长期运行时补充测试策略、部署与运维文档。
- **需求不明确**：缺少 PRD 或无法确认业务目标时，Agent 必须先向用户确认，不得自行编造业务规则。可以创建标注 `DRAFT` 或 `UNKNOWN` 的技术调查记录，但不得把它当成已批准的 PRD 或设计结论。

`PROJECT_STATUS.md` 是个人文档目录中的项目状态索引，不替代 PRD、设计文档或实施计划。文件不存在且满足上述创建条件时，Agent 应在首次实施前在个人 `.agent-docs/` 目录创建最小版本；没有既有路径约定时使用 `.agent-docs/project/PROJECT_STATUS.md`。文件至少包含：当前分支/版本、需求来源、活动 PRD/设计/实施文档链接、当前任务、已完成项、进行中事项、阻塞项、唯一下一步、最近验证、关联 commit、PR 或未提交变更状态、负责人和更新时间。每次任务结束，或活动计划、当前任务、阻塞项、下一步、验证结果发生变化时，都要更新它。

在公司项目中，必须遵守公司权限、保密、评审和发布制度。公司已有文件只读参考；需要个人记录时复制到当前项目的个人 `.agent-docs/` 目录，不修改、删除、重命名或覆盖源文件。个人 `.agent-docs/` 目录仅供个人维护，不替代公司的正式文档和审批记录；如保密制度不允许复制原文，只能在个人目录记录不含敏感内容的摘要和源文件路径。

## 8. 变更控制与验证基准

- **最小改动准则**：严格遵循最小作用域原则。未经用户明确确认，不得顺手进行任何无关重构、大范围格式化，不得擅自变更生产依赖、公共 API 或数据库语义。
- **验证分层覆盖**：前端可见改动必须执行页面/UI 呈现验证；后端逻辑、数据库与接口改动必须执行自动化测试或实际链路验证。
- **验证结论可证伪**：完成结论必须基于当前会话中实际跑通的验证结果；若受环境限制仅执行部分验证，必须在状态中如实标明未覆盖范围与剩余风险，严禁在未经验证时声称任务完成。

## 9. Bug排查

- 排查Bug时，先复现并记录运行环境、版本、操作步骤、预期结果和实际结果；无法复现时，不直接猜测修改。
- 按实际链路逐层取证：前端界面与控制台、网络请求与响应、后端入口与日志、数据库和外部依赖。已有 `request_id` 或 `trace_id` 时用其关联前后端日志；缺失且确实妨碍定位时，先确认再补充最小可用链路标识。日志不得记录密钥、令牌或完整敏感数据。
- 先用证据确定问题边界，再修改最小范围；不得同时盲改前后端。证据不足时写明缺少的证据。
- 能自动化时，修复前或修复时增加能够复现问题的失败测试；无法自动化时，记录可重复的人工验证步骤。修复后执行相关单元、集成或端到端回归测试，并将根因摘要、修改位置、验证结果和遗留风险同步到活动计划或状态文档。

## 10. Git与破坏性操作

除非用户明确要求，否则不执行Commit、Push、Clean、Amend、Rebase、Reset或强制推送。删除、覆盖、数据库清空和不可逆迁移前，先列出对象、影响、理由和恢复方式，获得确认后执行。

用户要求提交代码、执行 commit 或生成中文提交信息时：

- 优先分析 `git diff --cached`，不包含无关的 unstaged/untracked 文件。若当前没有 staged changes，但用户明确要求提交当前修改或全部改动时：AI 应先列出本次任务实际涉及的修改文件清单供用户确认，经确认后精准暂存相应文件，再自动生成 message 提交；**严禁未经确认直接执行盲目的 `git add .`** 混入无关改动或未跟踪文件。
- 目标项目已有提交规范时以项目规范为准；均未规定时使用 Conventional Commits（`feat|fix|docs|style|refactor|perf|test|chore|ci|build|revert`）：新功能用 `feat`，修复用 `fix`，文档用 `docs`，测试用 `test`，重构用 `refactor`，性能优化用 `perf`，依赖/构建用 `build`，CI 用 `ci`，其他维护用 `chore`。
- 格式为 `type(scope): subject`；scope 可选并从实际改动推断，subject 使用简洁中文动作描述，不超过 100 字符，不加句号。破坏性修改使用 `type(scope)!: subject`；彼此无关的改动先提示拆分提交。
- 自动生成 message 并执行 `git commit`，不要求用户手写；不自动 push、amend、rebase、reset 或使用 `--no-verify`，除非用户明确要求。
- 提交后检查状态并报告 commit hash。

## 11. Agent执行边界

Agent执行新功能、重要行为变化、架构调整，以及会影响公共接口、数据模型或模块边界的Bug修复和重构前，必须：

1. 找到对应的PRD、规格、架构文档、API/数据契约、数据字典、实施计划和项目状态文件。
2. 检查目标模块、直接调用方、已有API、数据模型和相关测试。
3. 说明本次修改对应的需求条目、架构节点、影响范围和非目标。
4. 检查是否已有等价功能；无法确认时标记为`UNKNOWN`，不得直接实现。
5. 读取现有验证方式，并确定本次修改需要执行的验证。

如果缺少PRD或无法确认业务目标，Agent必须向用户提出问题，不得自行补写或推断PRD内容。Agent可以先补充基于现有代码、日志和契约的技术调查记录，并将未确认内容标记为`UNKNOWN`。个人项目的小需求，以用户当次的明确说明为准，不强制补写PRD，但要在状态文件或功能说明中登记这条需求。

如果只是缺少规格、架构归属或验收标准，Agent可以先创建待确认的技术文档，但不得自行确定业务规则。

## 12. Agent代码修改规则

Agent修改代码时必须：

- 先读取目标文件的相关部分和调用链（小文件读全文），再编辑。
- 优先进行最小范围修改，不得顺手重构无关代码。
- 不得创建与现有功能重复的模块、API、类型、工具或数据处理逻辑。
- 不得虚构接口、字段、错误码、配置项或业务规则。
- 不得连续叠加未经验证的修复；每次独立修改或修复后先执行对应的快速验证，验证通过后再继续下一步。
- 发现现有代码无法解释时，先记录问题边界和未知项，再继续定位。
- 需要改变公共接口、数据结构、模块边界或生产依赖时，先更新对应的规格、架构文档、API/数据契约、数据字典和实施计划（经用户确认的探索性试验 Spike 除外，但验证通过后、正式合并前必须补齐）。

## 13. Agent验证与交付规则

Agent交付任务前必须：

1. 检查Diff，确认没有无关改动。
2. 执行与修改范围匹配的测试或人工验证。
3. 检查异常路径、边界条件、超时、重试、并发和资源释放。
4. 将实际验证结果、未验证内容和剩余风险写入项目状态或实施计划。
5. 按实际影响范围更新受影响的规格、契约、架构文档和数据字典。
6. 报告修改文件、验证命令、结果和未解决问题。

验证未完成时，Agent不得声称任务已完成。

## 14. 工具安装与环境配置

本地未安装 Superpowers（https://github.com/obra/superpowers）或 Understand Anything（https://github.com/Egonex-AI/Understand-Anything）时，不直接跳过，按以下步骤帮助用户从 GitHub 安装并配置：

1. 打开对应项目的 GitHub 仓库，按其官方安装说明确认安装方式、前置依赖和写入位置；命令和路径以仓库当前说明为准。
2. 安装方式有多种或需要全局权限时，先向用户说明将执行的操作再执行；方式明确且无系统级影响时可直接安装。
3. 安装后用一条最小命令验证可用（如查看版本或帮助）；需要配置的项按本规则默认值设置，例如知识图语言默认中文、结果导出到个人文档目录。
4. 安装失败、网络受限或用户不同意时，不反复重试，按第1节、第3节的回退方式用代码搜索、Git、调用链和测试继续任务，并告知用户未安装成功。
5. 一次性小任务和纯只读问答可先用回退方式完成任务，安装放到任务结束后处理。

配置只写用户级目录或个人 `.agent-docs/`，不写入项目共享文件，不修改工具仓库源文件。

