# AGENTS.md

## 1. 新会话与断点续作

开始处理项目任务时，按需读取：

1. `README.md`
2. 项目状态文档（已有则读取；没有则按“长期项目状态”规则判断是否创建）
3. 状态文件指向的活动规格和计划
4. 与当前任务相关的契约、ADR、数据字典、代码和测试
5. Git状态、当前Diff和最近相关提交

若用户提出新的独立任务，以新任务为准。当用户明确要求继续当前项目但未指定计划时：若Superpowers可用且只有一个未完成实施计划，则使用其 `subagent-driven-development` 工作流按该计划续作；存在多个候选、记录冲突或无法判断时才询问用户。

执行前确认当前分支、活动计划、已完成项、阻塞项、下一步和未提交修改。文档可能过期，必须用代码、测试和Git交叉验证；不重复已有证据证明完成的任务。

事实来源：

- 目标行为：已批准的规格、API契约、ADR和数据字典。
- 当前实现：代码、配置、Schema、Migration、测试和实际运行结果。
- 执行进度：`PROJECT_STATUS.md`和Git记录；Superpowers规格和计划只表示目标与步骤，不单独证明已经完成。
- 来源冲突时列出差异及影响；无法确认的信息标记为 `UNKNOWN`，不自行猜测。

项目已有等价文档、目录或命名规范时优先沿用；本文路径仅作为默认值，不重复创建。

## 2. 长期项目状态

预计持续多个会话或包含多个计划的项目，维护项目已有的状态文档；没有等价文档时，默认使用 `docs/project/PROJECT_STATUS.md`。一次性任务和纯只读问答无需创建或更新。

文件不存在时，在首次实施前根据已验证的仓库状态创建最小版本，只记录：

- 当前版本或分支、需求来源、活动规格、活动计划、当前任务
- 最近完成、进行中、阻塞项、唯一下一步、最近验证

仅在活动计划、当前任务、阻塞项、下一步或验证结果发生实际变化时更新。结束或清理Superpowers创建的工作区前，先同步完成项、遗留问题、下一步和最近验证。

状态文件只保存当前摘要和文档指针；完整需求、设计、执行细节和历史分别保留在规格、计划、ADR和Git中。

## 3. 项目知识图

大型、长期或多模块项目，若Understand Anything可用，使用其知识图作为模块索引：

- 已有 `.ua/knowledge-graph.json` 或 `.understand-anything/knowledge-graph.json` 时直接沿用；已有人工模块文档继续保留，不重复创建 `MODULE_INDEX.md`。知识图记录项目结构和依赖，`PROJECT_STATUS.md`记录执行进度，二者不互相替代。
- 知识图缺失时：现有项目在首次功能迭代或Bug修复前运行 `$understand`；新项目在主要目录和模块形成后运行。语言默认使用中文，需要额外完整性复核时才加 `--review`；空仓库、一次性小任务和纯只读问答可跳过。首次扫描前检查 `.understandignore` 和项目现有忽略规则。
- 功能迭代或Bug排查前，先用 `$understand-chat <功能或问题>` 定位入口、相关模块、上下游依赖和测试，再读取实际代码、契约、日志和测试；知识图只用于导航和影响分析，不作为当前实现的唯一证据。
- 知识图落后于当前提交时，先运行 `$understand` 增量更新；工作区存在未提交修改时，将知识图视为基线，并同时检查Git Diff和实际代码。
- 修改完成后使用 `$understand-diff` 检查影响范围。工具未安装、执行失败或图不完整时，改用代码搜索、调用链、Git和测试继续定位。
- `.ua/`或`.understand-anything/`是否提交、忽略哪些内容以及是否启用 `$understand --auto-update`，遵循项目现有规范；未经确认不修改Git钩子或提交生成物。

## 4. 数据库设计

涉及表、字段、索引、约束或关系变化时，同一任务必须同步：

- Schema或ORM模型
- Migration及回滚方案；不可逆时明确说明
- `docs/data/DATA_DICTIONARY.md`
- 受影响的规格、API契约、共享类型和自动测试

数据字典至少记录字段类型、可空与默认值、键与索引、业务用途、数据来源、约束、敏感性、迁移兼容性及关联任务。另行说明旧数据回填，以及迁移对性能、锁和并发的影响。遵循现有迁移规范，不直接修改已发布Migration。

## 5. API与架构决策

API变化必须同步现有契约、共享类型、请求响应、错误码、认证权限、兼容性、示例和契约测试。前端不得虚构后端字段；后端不得在未更新契约时改变响应。破坏性变更实施前必须确认。

引入重要生产依赖，或改变核心数据模型、模块边界、认证权限、基础设施时，创建 `docs/architecture/adr/ADR-NNN-<title>.md`，记录背景、决策、原因、备选方案、影响、风险、回滚方式和关联任务。普通实现细节保留在项目规格或实施计划中。

## 6. 范围与验证

- 修改前检查目标文件、直接调用方、相关测试和现有实现，控制最小改动范围。
- 未经确认，不进行无关重构、大范围格式化，也不改变生产依赖、公共API或数据库语义。
- 完成结论必须基于当前任务中实际执行的验证；只完成部分验证时明确未执行范围。
- 前端可见变化执行浏览器验证；后端、数据库和API变化执行相应集成或契约测试。

## 7. Bug排查

- 排查Bug时，先复现并记录运行环境、版本、操作步骤、预期结果和实际结果；无法复现时，不直接猜测修改。
- 按实际链路逐层取证：前端界面与控制台、网络请求与响应、后端入口与日志、数据库和外部依赖。已有 `request_id` 或 `trace_id` 时用其关联前后端日志；缺失且确实妨碍定位时，先确认再补充最小可用链路标识。日志不得记录密钥、令牌或完整敏感数据。
- 先用证据确定问题边界，再修改最小范围；不得同时盲改前后端。证据不足时写明缺少的证据。
- 能自动化时，修复前或修复时增加能够复现问题的失败测试；无法自动化时，记录可重复的人工验证步骤。修复后执行相关单元、集成或端到端回归测试，并将根因摘要、修改位置、验证结果和遗留风险同步到活动计划或状态文档。

## 8. Git与破坏性操作

Git提交优先遵循当前项目规范，其次遵循可用的上级或全局 `AGENTS.md`；均未规定时使用Conventional Commits（`type(scope): 简洁中文subject`）。除非用户明确要求，否则不执行Commit、Push、Clean、Amend、Rebase、Reset或强制推送。

删除、覆盖、数据库清空和不可逆迁移前，先列出对象、影响、理由和恢复方式，获得确认后执行。
